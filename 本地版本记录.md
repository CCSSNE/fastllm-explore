# 本地版本记录

必须使用 UTF-8 编码保存本文件。

## 第一原始版

- 时间：2026-05-10 15:16:20 +01:00
- 本地提交哈希：`e42ba04a23d6816e097f6d0f5f90e0fd63fca63e`
- 短哈希：`e42ba04`
- 本地标签：`first-original-q8-baseline`
- 说明：将当前从 `C:\Users\user\output\deepseek-v4部署` 移到 `D:\AI\LLM\deepseek-v4部署` 后仍可跑通的状态定为本地第一原始版。
- 验证：Q8 模型 `F:\下载\cyberneurova-DeepSeek-V4-Flash-abliterated-Q8_0.gguf` 可加载，fastllm 识别 `cpu/cuda/disk`，最小测试可输出 token。
- 结果：输入 `请回答：你好`，输出 token 为 `你好`、`！`，最终输出 `你好！`。
- 备注：退出阶段出现 `cudaErrorCudartUnloading / driver shutting down`，进程退出码为 0，发生在 token 输出之后，暂记录为退出释放 CUDA 内存时的噪声。
- 回看此版本：`git show e42ba04` 或 `git show first-original-q8-baseline`
- 从此版本建分支：`git switch -c 分支名 first-original-q8-baseline`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard first-original-q8-baseline`

## 记录修正

- 时间：2026-05-10 15:17:19 +01:00
- 说明：补写第一原始版的完整提交哈希、短哈希、本地标签和常用回退/建分支命令。没有修改运行代码。

## Q8 预填充块优化

- 时间：2026-05-10 15:37:13 +01:00
- 本地提交哈希：`c46801632eae189d4fe6781e8738a145f035c1cc`
- 短哈希：`c468016`
- 本地标签：`q8-chunked-prefill-8`
- 改动：`model_runtime.py` 新增 `DSV4_CHUNKED_PREFILL_SIZE` / `Q2_CHUNKED_PREFILL_SIZE` 配置，并将默认 `chunked_prefill_size` 从 `1` 调为 `8`；`.gitignore` 忽略本次 Q8 性能验证日志。
- 磁盘实测：`F:` 对应 Disk 2 `Lexar SS D ARES 4TB`，BusType 为 `USB`。Q8 文件冷读顺序速度约 `881.7 MB/s`，unbuffered 随机/大块多线程读最高约 `999.0 MB/s`。
- 解释：`F:` 当前不是内置 NVMe 路径，而是 USB 外接盘；USB 3.0 的 `5Gbps` 是约 `625MB/s` 的理论值，不是 `5GB/s`。实测约 `0.9-1.0GB/s` 符合外接 SSD 链路表现，不能按内置 PCIe 4.0 `7000MB/s` 预期。
- 运行瓶颈：Q8 Disk MoE 按 token、按层加载专家权重，读盘模式不是单纯大块顺序扫描；profile 显示单次 1 token 前有多次 forward，MoE 段占主要时间，CPU 满载说明还有解量化/矩阵计算瓶颈。
- 验证前：`chunked_prefill_size=1`，输入 `请回答：你好`，1 token 输出 `你好`，`done_sec=103.4`。
- 验证后：`chunked_prefill_size=8`，输入 `请回答：你好`，1 token 输出 `你好`，`done_sec=74.1`。
- 构建阻塞：尝试重编译 C++ fastllm 时发现迁移后的 CMake cache 仍指向 `C:\Users\user\output\...`，且本机没有找到 Visual Studio/MSVC `cl.exe` / `VsDevCmd.bat`。因此没有提交未编译验证的 C++ Disk MoE 读盘并发补丁。
- 回看此版本：`git show c468016` 或 `git show q8-chunked-prefill-8`
- 从此版本建分支：`git switch -c 分支名 q8-chunked-prefill-8`

## Q8 本地 NVMe 模型默认路径

- 时间：2026-05-10 20:25:12 +01:00
- 本地提交哈希：`9c31b8c8edc0229ea76634adfe1f24bd62a97f48`
- 短哈希：`9c31b8c`
- 本地标签：`q8-local-nvme-default`
- 改动：`model_runtime.py` 默认优先使用当前工作目录下的 `cyberneurova-DeepSeek-V4-Flash-abliterated-Q8_0.gguf`，不存在时再回退到旧的 `F:\下载\...` 路径；`.gitignore` 忽略本地 302GB Q8 GGUF 和本次验证日志。
- 盘符确认：`D:` 对应 Disk 1 `Lexar SSD ARES 4TB`，BusType 为 `NVMe`。
- 磁盘实测：D 盘 Q8 文件 unbuffered 顺序冷读约 `4810.2 MB/s`；1MB 随机/多线程读最高约 `6139.5 MB/s`；8MB 随机/多线程读最高约 `6273.6 MB/s`。
- 验证：不设置 `DSV4_MODEL_PATH` 时自动选择 `D:\AI\LLM\deepseek-v4部署\cyberneurova-DeepSeek-V4-Flash-abliterated-Q8_0.gguf`。
- 结果：输入 `请回答：你好`，1 token 输出 `你好`，`loaded_sec=18.6`，`done_sec=48.5`。
- 对比：此前外接盘 `F:` + `chunked_prefill_size=8` 同测试 `done_sec=74.1`，移动到 D 盘 NVMe 后约快 `34.5%`。
- 回看此版本：`git show 9c31b8c` 或 `git show q8-local-nvme-default`
- 从此版本建分支：`git switch -c 分支名 q8-local-nvme-default`

## Q8 长输出解码读盘监控与 Disk MoE 线程优化

- 时间：2026-05-10 21:02:46 +01:00
- 本地提交哈希：`29aa86301530c5d6231978ea7969001e48dc88c9`
- 短哈希：`29aa863`
- 本地标签：`q8-disk-moe-load-threads-16`
- 改动：`model_runtime.py` 默认设置 `FASTLLM_DISK_MOE_LOAD_THREADS=16`；`.gitignore` 忽略长输出解码 profile 日志和读盘监控 CSV。
- 验证模型：默认路径为 `D:\AI\LLM\deepseek-v4部署\cyberneurova-DeepSeek-V4-Flash-abliterated-Q8_0.gguf`，即 D 盘本地 NVMe 上的 Q8 模型。
- 长输出吐 token 监控：`DSV4_THREADS=16`、`FASTLLM_DSV4_PROFILE=1`、`DSV4_MAX_NEW_TOKENS=6`，输出 token 片段为 `今天`、`对`、`本地`、`部署`、`的`、`模型`。
- token 时间：首 token `130.935s`；后续 token 间隔约 `9.982s`、`9.302s`、`9.219s`、`8.601s`、`8.698s`；总 `done_sec=176.7`。
- 吐 token 期间读盘：D 盘读盘监控共 `98` 个样本，平均约 `559.3 MB/s`，最大约 `1323.5 MB/s`；最后 30 个样本平均约 `451.6 MB/s`，最大约 `681.5 MB/s`。
- 对照磁盘能力：同一 D 盘 Q8 文件 unbuffered 顺序冷读约 `4810.2 MB/s`，1MB/8MB 多线程随机读最高约 `6.1-6.3 GB/s`。因此吐 token 时只用到数百 MB/s，不是 NVMe 物理吞吐上限。
- 线程扫描：`FASTLLM_DISK_MOE_LOAD_THREADS=1` 时 `done_sec=154.4`；`4` 时 `156.2`；`8` 时 `160.6`；`16` 时 `135.4`。默认 16 是当前实测最好的轻量参数，但只能小幅改善，不是根治。
- 判断：你的重点方向是对的，应该看解码吐 token 时的读盘，而不是只看模型加载前的冷读。但现在更准确的瓶颈不是“磁盘跑不满所以磁盘慢”，而是 Disk MoE 解码路径没有把 NVMe 喂满：每 token、每层、按专家临时读权重，读请求偏碎且受 CPU 解量化/矩阵计算节奏限制。
- 源码怀疑点：`fastllm-master\src\devices\disk\diskdevice.cpp` 的 Windows 读盘路径使用 `_lseeki64` + `_read` 并带全局读锁；Linux 分支是 `pread`。这会限制 Windows 多线程随机读权重的并发性。因为当前本机缺少可用 MSVC `cl.exe` / VS 构建环境，本次没有提交未编译验证的 C++ 改动。
- 下一步优化方向：优先做可编译验证的 Windows Disk MoE 并发读补丁，例如每线程独立文件句柄、等价 `pread` 的 offset read、或 `ReadFile` 指定 offset；其次考虑热专家缓存、下一层/下一专家预取、以及在内存/显存允许时固定驻留高频专家。
- 回看此版本：`git show 29aa863` 或 `git show q8-disk-moe-load-threads-16`
- 从此版本建分支：`git switch -c 分支名 q8-disk-moe-load-threads-16`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-disk-moe-load-threads-16`

## Windows Disk MoE 并发读源码补丁

- 时间：2026-05-10 21:14:00 +01:00
- 本地提交哈希：`e19c182415230dc5524a6e35258626180c16387c`
- 短哈希：`e19c182`
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 在 Windows 下把 Disk MoE 文件句柄缓存改为 `thread_local DiskFileCache`，每个加载线程拥有自己的 fd；移除 Windows 读盘路径里的全局 `readLocker`。
- 目的：解决 Windows `_lseeki64` + `_read` 共享文件指针时必须串行 seek/read 的问题，让 `FASTLLM_DISK_MOE_LOAD_THREADS=16` 真正具备并发读权重的机会。
- 保留行为：Linux / 非 Windows 分支仍使用共享 fd 缓存和 `pread`，因为 `pread` 本身不移动共享文件指针。
- 静态验证：`git diff --check` 通过，没有发现空白错误。
- 构建验证：尝试运行旧 `build_cuda_fastllm.cmd` 失败，原因一是脚本硬编码旧的 `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\...` 路径，二是现有 `build-cuda-ninja` 的 CMake cache 仍指向迁移前的 `C:\Users\user\output\...`。
- 编译器状态：`vswhere` 找到 Visual Studio Build Tools 2026 位于 `E:\program\VisualStudio`，但 `VsDevCmd.bat` 环境中 `where cl` 失败，且 `vswhere -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64` 没有返回安装路径；当前无法本机生成新的 `fastllm_tools.dll`。
- 重要说明：这个提交是源码优化待编译版，当前运行时仍使用旧的 `fastllm-master\build-cuda-ninja\tools\ftllm\fastllm_tools.dll`，所以还没有实测 token/s 改善。
- 下一步验证条件：安装或启用 MSVC C++ x64 tools 后，用干净构建目录重新 CMake 配置当前 D 盘源码，编译 `fastllm_tools`，再用同一个长输出脚本对比吐 token 时 D 盘读盘速率和 token 间隔。
- 回看此版本：`git show e19c182`
- 从此版本建分支：`git switch -c 分支名 e19c182`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard e19c182`

## Q8 构建与长输出验证闭环脚本

- 时间：2026-05-10 21:21:45 +01:00
- 本地提交哈希：`9123e7b55e22ce8c18debac7a67c9f4cfd96c30d`
- 短哈希：`9123e7b`
- 改动：新增 `build_cuda_fastllm_clean.cmd`、`verify_q8_decode_tokens.py`、`verify_q8_decode_disk.ps1`，并在 `.gitignore` 忽略 `fastllm-master/build-cuda-ninja-clean/`。
- 目的：把之后的优化流程固定成可复跑闭环：干净构建 `fastllm_tools.dll`，再跑同一套 Q8 长输出逐 token 计时和 D 盘读盘监控。
- 构建脚本：`build_cuda_fastllm_clean.cmd` 使用 `vswhere` 查找带 `Microsoft.VisualStudio.Component.VC.Tools.x86.x64` 的 VS 安装，使用当前工作目录下的 `cuda-12.4-toolkit` 和干净构建目录 `fastllm-master\build-cuda-ninja-clean`。
- 验证脚本：`verify_q8_decode_tokens.py` 输出 `STATUS`、`loaded_sec`、逐 token `TOKEN_EVENT`、`done_sec` 和最终输出；`verify_q8_decode_disk.ps1` 同步采样 `\PhysicalDisk(1 D:)` 读盘速率并汇总平均/最大读速。
- 已做校验：`python -m py_compile .\verify_q8_decode_tokens.py .\model_runtime.py` 通过；`verify_q8_decode_disk.ps1` 用 `[scriptblock]::Create(...)` 解析通过；`git diff --check` 通过。
- 构建尝试结果：运行 `cmd /c build_cuda_fastllm_clean.cmd` 失败并明确报错 `MSVC C++ x64 tools are not installed`，需要安装组件 `Microsoft.VisualStudio.Component.VC.Tools.x86.x64` 后再继续。
- 为什么没有继续跑长输出对比：当前还没生成包含 `e19c182` 源码补丁的新 `fastllm_tools.dll`，直接跑长输出只会测试旧 DLL，不能证明 C++ Disk MoE 读盘补丁有效。
- 恢复闭环命令：C++ tools 安装好后，先运行 `cmd /c build_cuda_fastllm_clean.cmd`；构建成功后替换运行 DLL 或调整 `model_runtime.py` 的 `TOOLS_DIR` 指向新构建目录，再运行 `powershell -ExecutionPolicy Bypass -File .\verify_q8_decode_disk.ps1`。
- 回看此版本：`git show 9123e7b`
- 从此版本建分支：`git switch -c 分支名 9123e7b`

## Q8 Windows Disk MoE clean DLL 实测提速版

- 时间：2026-05-10 21:52:07 +01:00
- 本地提交哈希：`8f0fe8b873de3208fd1a70a8481732e0d66a30dd`
- 短哈希：`8f0fe8b`
- 本地标签：`q8-windows-disk-moe-clean-dll-verified`
- 环境修复：通过提升权限运行 Visual Studio Installer，给 `E:\program\VisualStudio` 增加 C++ x64 tools；确认 `cl.exe` 位于 `E:\program\VisualStudio\VC\Tools\MSVC\14.50.35717\bin\Hostx64\x64\cl.exe`。
- 构建结果：`cmd /c build_cuda_fastllm_clean.cmd` 成功，生成新 DLL `fastllm-master\build-cuda-ninja-clean\tools\ftllm\fastllm_tools.dll`，大小 `29,754,368` 字节。
- 改动：`model_runtime.py` 优先加载 `build-cuda-ninja-clean\tools` 中的新 DLL，找不到时再回退旧 `build-cuda-ninja\tools`；`status()` 增加 `tools_dir`，避免后续误测旧 DLL；`verify_q8_decode_disk.ps1` 改为可由 Windows PowerShell 直接执行。
- 验证确认：运行状态显示 `tools_dir=D:\AI\LLM\deepseek-v4部署\fastllm-master\build-cuda-ninja-clean\tools`，说明实测加载的是包含 Windows thread-local fd Disk MoE 补丁的新 DLL。
- 6 token 正式对比：旧 DLL `done_sec=176.7`；新 DLL `done_sec=88.1`。首 token 从 `130.935s` 降到 `64.127s`；后续 token 从约 `8.6-10.0s` 降到约 `4.49-5.16s`。
- 吐 token 期间读盘：旧 DLL D 盘平均约 `559.3 MB/s`、最大约 `1323.5 MB/s`；新 DLL D 盘平均约 `999.3 MB/s`、最大约 `1995.6 MB/s`，最后 30 个样本平均约 `1162.7 MB/s`。
- 结论：Windows `_lseeki64` + `_read` 共享 fd 全局锁确实是主要瓶颈之一；改为每线程 fd 后，Q8 长输出速度约提升一倍，但仍未跑满 NVMe，后续瓶颈转向 Disk MoE 请求形态、CPU 解量化/矩阵计算和专家加载策略。
- 线程扫描：在新 DLL 下继续扫 `FASTLLM_DISK_MOE_LOAD_THREADS=24/32`，3 token 结果分别为 `71.0s` 和 `74.2s`；没有优于当前默认 `16`，因此继续保留默认 16。
- 回看此版本：`git show 8f0fe8b` 或 `git show q8-windows-disk-moe-clean-dll-verified`
- 从此版本建分支：`git switch -c 分支名 q8-windows-disk-moe-clean-dll-verified`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-windows-disk-moe-clean-dll-verified`

## Q8 16 token 稳定测速与默认线程优化

- 时间：2026-05-10 22:57:23 +01:00
- 本地提交哈希：`934d584e254875727920277282809d4029c4749c`
- 短哈希：`934d584`
- 本地标签：`q8-decode-threads-16-clean-benchmark`
- 改动：`model_runtime.py` 将默认 CPU 线程数从 `32` 改为 `16`；`verify_q8_decode_disk.ps1` 新增 `-Profile` 开关，默认测速不再开启 `FASTLLM_DSV4_PROFILE`，避免每层 profile 日志污染性能基准。
- 用户基准：之后固定用 `MaxNewTokens=16` 做稳定测速。命令为 `powershell -NoProfile -ExecutionPolicy Bypass -File .\verify_q8_decode_disk.ps1 -MaxNewTokens 16`。
- 对比验证：新 clean DLL、`FASTLLM_DISK_MOE_LOAD_THREADS=16`、`chunked_prefill_size=8`。`DSV4_THREADS=32` 时 16 token `done_sec=338.9`，D 盘平均读速约 `496.9 MB/s`、最后 30 样本平均约 `403.0 MB/s`。
- 优化结果：`DSV4_THREADS=16` 时 16 token `done_sec=137.1`，首 token `62.603s`，后续 token 约 `4.27-5.26s/token`；D 盘平均读速约 `1100.8 MB/s`、最大约 `2097.1 MB/s`、最后 30 样本平均约 `1131.5 MB/s`。
- 判断：默认 32 线程在当前 Windows + Disk MoE 解码路径下会加重 CPU/磁盘加载线程竞争，实际比 16 线程慢很多。默认 16 更接近当前机器和 Q8 Disk MoE 的平衡点。
- 备注：测速进程退出阶段仍会打印 `cudaErrorCudartUnloading / driver shutting down`，发生在 token 输出完成之后；本次验证退出码为 0，暂按 CUDA 释放阶段噪声处理。
- 回看此版本：`git show 934d584` 或 `git show q8-decode-threads-16-clean-benchmark`
- 从此版本建分支：`git switch -c 分支名 q8-decode-threads-16-clean-benchmark`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-decode-threads-16-clean-benchmark`

## Q8 16 token CPU 线程数细扫

- 时间：2026-05-10 23:12:55 +01:00
- 本地提交哈希：`8e6e1cecce2466639601cefc069bafd31c6c8874`
- 短哈希：`8e6e1ce`
- 本地标签：`q8-decode-threads-12-benchmark`
- 改动：`model_runtime.py` 默认 CPU 线程数从 `16` 改为 `12`；`verify_q8_decode_disk.ps1` 默认 `Threads` 也同步改为 `12`，让服务端默认和 16 token 验证脚本口径一致。
- 线程扫描：新 clean DLL、`FASTLLM_DISK_MOE_LOAD_THREADS=16`、`MaxNewTokens=16`。`threads=12` 最好单次 `done_sec=126.6`，D 盘平均读速约 `1142.3 MB/s`、最后 30 样本平均约 `1250.5 MB/s`。
- 对照结果：`threads=8` 为 `done_sec=138.3`；`threads=16` 有两次分别约 `137.1s` 和 `132.7s`；`threads=32` 为 `338.9s`。因此 32 明显过载，8 偏少，12/16 接近但 12 当前略优。
- 默认复测：不显式传 `-Threads`，确认 `status()` 显示 `threads=12`；16 token `done_sec=135.6`，首 token `62.005s`，后续 token 约 `4.38-5.14s/token`；D 盘平均读速约 `1115.3 MB/s`、最大约 `1820.3 MB/s`、最后 30 样本平均约 `1256.7 MB/s`。
- 判断：这轮是小幅参数优化，主要价值是避开 32 线程造成的严重 CPU/磁盘调度竞争；12 相比 16 的收益受运行波动影响，不按大幅提速记录。
- 回看此版本：`git show 8e6e1ce` 或 `git show q8-decode-threads-12-benchmark`
- 从此版本建分支：`git switch -c 分支名 q8-decode-threads-12-benchmark`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-decode-threads-12-benchmark`

## Q8 Disk MoE 热权重缓存与 8GB 默认缓存

- 时间：2026-05-10 23:45:52 +01:00
- 本地提交哈希：`b76aaf3e725205873d9e20504411ad1373d1b56c`
- 短哈希：`b76aaf3`
- 本地标签：`q8-disk-moe-cache-8gb`
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 新增有上限的 Disk MoE 已加载权重 LRU 缓存，由 `FASTLLM_DISK_MOE_CACHE_MB` 控制；缓存命中时复用已加载的专家权重，未命中时仍按原路径从磁盘加载。
- 默认配置：`model_runtime.py` 默认设置 `FASTLLM_DISK_MOE_CACHE_MB=8192`，并在 `status()` 中输出 `disk_moe_cache_mb`；`verify_q8_decode_disk.ps1` 默认 `MaxNewTokens` 从 `6` 改为 `16`，以后不传参数也按 16 token 稳定测速。
- 构建验证：运行 `cmd /c build_cuda_fastllm_clean.cmd` 成功，生成 `fastllm-master\build-cuda-ninja-clean\tools\ftllm\fastllm_tools.dll`；`python -m py_compile .\model_runtime.py .\verify_q8_decode_tokens.py` 通过；`verify_q8_decode_disk.ps1` 解析通过；`git diff --check` 通过。
- cache off 对照：`FASTLLM_DISK_MOE_CACHE_MB=0` 时，16 token `done_sec=124.2`，首 token `58.7s`，后 15 token 合计约 `65.5s`，整体约 `0.1288 token/s`；D 盘平均读速约 `1222.3 MB/s`，最大约 `2264.8 MB/s`。
- 4GB cache 对照：`FASTLLM_DISK_MOE_CACHE_MB=4096` 时，16 token `done_sec=118.7`，首 token `53.1s`，后 15 token 合计约 `65.7s`，整体约 `0.1348 token/s`；收益主要来自首 token，后续 token 基本没变。
- 8GB cache 扫描：`FASTLLM_DISK_MOE_CACHE_MB=8192` 时，16 token 最好 `done_sec=103.8`，首 token `53.4s`，后 15 token 合计约 `50.4s`，后续速度约 `0.298 token/s`；D 盘平均读速约 `1165.7 MB/s`，最大约 `1991.2 MB/s`。
- 12GB cache 扫描：`FASTLLM_DISK_MOE_CACHE_MB=12288` 时，16 token `done_sec=111.5`，没有优于 8GB；因此默认不继续加大到 12GB，避免多占内存但收益不稳定。
- 最终默认复测：不手动设置 cache 环境变量，确认 `status()` 显示 `disk_moe_cache_mb='8192'`；16 token `done_sec=104.6`，首 token `54.2s`，后 15 token 合计约 `50.4s`；D 盘平均读速约 `1106.1 MB/s`，最大约 `1774.6 MB/s`。
- 对用户长测基准的意义：用户之前长测约 `0.12 token/s`；当前 16 token 默认 8GB cache 整体约 `0.153 token/s`，排除首 token 后约 `0.298 token/s`。短测会受首 token 和提示长度影响，长输出后段速度更应看后续 token 间隔。
- 判断：8GB cache 说明 Disk MoE 热专家权重确实存在可复用空间，但这不是把 NVMe 跑满的终局方案；后续更值得继续做的是按层/按专家的命中率统计、预取策略和更细的 CPU 解量化/矩阵计算瓶颈拆分。
- 开关方式：需要临时关闭缓存可在运行前设置 `FASTLLM_DISK_MOE_CACHE_MB=0`；需要重新扫容量可设置为 `4096`、`8192`、`12288` 等 MB 数值。
- 备注：测速进程退出阶段仍会打印 `cudaErrorCudartUnloading / driver shutting down`，发生在 token 输出完成之后，当前按 CUDA runtime 卸载期释放噪声记录。
- 回看此版本：`git show b76aaf3` 或 `git show q8-disk-moe-cache-8gb`
- 从此版本建分支：`git switch -c 分支名 q8-disk-moe-cache-8gb`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-disk-moe-cache-8gb`

## Q8 8GB 缓存下 Disk MoE 加载线程数复扫

- 时间：2026-05-10 23:51:10 +01:00
- 本地提交哈希：`de0413b5badce0ff01cde3d46c2e95da14c6b969`
- 短哈希：`de0413b`
- 本地标签：`q8-disk-moe-cache-8gb-loadthreads-12`
- 改动：`model_runtime.py` 默认 `FASTLLM_DISK_MOE_LOAD_THREADS` 从 `16` 改为 `12`；`verify_q8_decode_disk.ps1` 默认 `DiskMoeLoadThreads` 同步从 `16` 改为 `12`。
- 背景：8GB Disk MoE 热权重缓存启用后，加载线程数的最优点可能变化，因此在固定 `MaxNewTokens=16`、CPU threads=12、cache=8192MB 下重新扫加载线程。
- 对照结果：`DiskMoeLoadThreads=8` 时 16 token `done_sec=110.5`；`DiskMoeLoadThreads=12` 手动扫描最好 `done_sec=101.3`；旧默认 `16` 的默认复测约 `104.6`，三者差距不大但 12 略优。
- 最终默认复测：不手动传 `-DiskMoeLoadThreads`，确认默认口径为 12；16 token `done_sec=105.6`，首 token `53.8s`，后 15 token 合计约 `51.9s`；D 盘平均读速约 `1160.4 MB/s`，最大约 `2174.2 MB/s`。
- 判断：12/16 在 8GB cache 下非常接近，12 的收益受运行波动影响，不按大幅提速记录；选择 12 是因为它在扫描中略优，并且 CPU/读盘调度压力更节制。
- 当前推荐默认组合：`DSV4_THREADS=12`、`FASTLLM_DISK_MOE_LOAD_THREADS=12`、`FASTLLM_DISK_MOE_CACHE_MB=8192`、`DSV4_CHUNKED_PREFILL_SIZE=8`、`MaxNewTokens=16` 用于小样本稳定测速。
- 回看此版本：`git show de0413b` 或 `git show q8-disk-moe-cache-8gb-loadthreads-12`
- 从此版本建分支：`git switch -c 分支名 q8-disk-moe-cache-8gb-loadthreads-12`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-disk-moe-cache-8gb-loadthreads-12`

## Q8 Disk MoE 缓存统计仪表盘

- 时间：2026-05-11 00:23:36 +01:00
- 本地提交哈希：`695ce9d25ada784459aa7194e494c501e262e6c5`
- 短哈希：`695ce9d`
- 本地标签：暂未打标签
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 增加可选 Disk MoE 统计日志。默认关闭；设置 `FASTLLM_DISK_MOE_STATS=1` 后，会按 `FASTLLM_DISK_MOE_STATS_EVERY` 间隔打印调用数、topk、每次实际加载权重数、缓存命中/未命中、驱逐、缓存占用、累计 miss 读盘量和热门 routed expert。
- 构建验证：`cmd /c build_cuda_fastllm_clean.cmd` 成功，生成 clean DLL；`git diff --check` 通过。
- 关闭统计的默认复测：`MaxNewTokens=16`、默认 `DSV4_THREADS=12`、`FASTLLM_DISK_MOE_LOAD_THREADS=12`、`FASTLLM_DISK_MOE_CACHE_MB=8192`，日志 `资料\验证结果\q8_long_decode_20260511_001639.log`。结果 `loaded_sec=28.6`、`done_sec=105.6`，与上一版默认复测基本持平，说明统计开关关闭时没有可见性能回退。
- 开启统计的复测：`FASTLLM_DISK_MOE_STATS=1`、`FASTLLM_DISK_MOE_STATS_EVERY=16`，日志 `资料\验证结果\q8_long_decode_20260511_001904.log`。结果 `loaded_sec=20.0`、`done_sec=102.5`，D 盘平均读速约 `1181.6 MB/s`、最后 30 样本平均约 `1342.3 MB/s`。
- 统计结论：16 token 测试末尾累计 `calls=768`、`tokens=1628`、`avgTopk=6.00`、`avgRoutedUnique=9.35`、`avgLoadWeights=18.70`、`cacheHit=2932`、`cacheMiss=11416`、命中率约 `20.43%`、缓存峰值约 `8185.5 MB`、累计 miss 读盘约 `145545.5 MB`。8GB cache 已经满载但命中率仍偏低，说明 routed expert 分散导致 LRU 一直驱逐，后续优化重点应继续放在 Windows offset read、预取/加载形态和 CPU 解量化/矩阵计算拆分上。
- 质量约束：本次没有改 `num_experts_per_tok`，DeepSeek-V4-Flash 仍使用官方 `6` 个 routed experts；没有降低 Q8 精度。
- 回看此版本：`git show 695ce9d`
- 从此版本建分支：`git switch -c 分支名 695ce9d`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 695ce9d`

## Q8 Windows Disk MoE offset ReadFile 读取路径

- 时间：2026-05-11 00:31:55 +01:00
- 本地提交哈希：`148fb4fd8c045e2af6ccd86804ff39bcc0f1f239`
- 短哈希：`148fb4f`
- 本地标签：暂未打标签
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 的 Windows Disk MoE 读取从 `_lseeki64 + _read` 改为 `_get_osfhandle + ReadFile`，并通过 `OVERLAPPED.Offset/OffsetHigh` 指定读取位置，减少每个权重分片读取前的单独 seek 调用。Linux 路径继续使用 `pread`。
- 构建验证：`cmd /c build_cuda_fastllm_clean.cmd` 成功，生成 clean DLL；`git diff --check` 通过。编译输出仍有既有 warning，没有新增阻断错误。
- 默认 16 token 复测：关闭统计，日志 `资料\验证结果\q8_long_decode_20260511_002650.log`。结果 `loaded_sec=22.2`、`done_sec=101.8`，首 token `52.389s`，后 15 token 合计约 `49.37s`；D 盘平均读速约 `1161.2 MB/s`、最后 30 样本平均约 `1331.3 MB/s`。
- 开启统计复测：`FASTLLM_DISK_MOE_STATS=1`、`FASTLLM_DISK_MOE_STATS_EVERY=16`，日志 `资料\验证结果\q8_long_decode_20260511_002911.log`。结果 `loaded_sec=20.4`、`done_sec=102.8`；D 盘平均读速约 `1167.6 MB/s`、最大约 `2102.5 MB/s`、最后 30 样本平均约 `1297.0 MB/s`。
- 统计对照：末尾仍是 `calls=768`、`avgTopk=6.00`、`cacheHit=2931`、`cacheMiss=11417`、命中率约 `20.43%`、累计 miss 读盘约 `145562.5 MB`。说明本次没有改变专家选择或缓存行为，只是降低 Windows offset 读取路径开销。
- 判断：相对上一版默认 `done_sec=105.6`，这轮稳定改善约 `3-4s`，属于小幅但正向的系统调用路径优化；仍未解决 8GB LRU 命中率只有约 20% 的根本问题。
- 质量约束：本次没有改 `num_experts_per_tok`，没有降低 Q8 精度，没有改变模型输出采样设置。
- 回看此版本：`git show 148fb4f`
- 从此版本建分支：`git switch -c 分支名 148fb4f`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 148fb4f`

## Q8 Disk MoE 分段 profile 诊断工具

- 时间：2026-05-11 01:49:04 +01:00
- 本地提交哈希：`1f9bc17d1ce51f3ac7efb2fe3cb983768316356b`
- 短哈希：`1f9bc17`
- 本地标签：暂未打标签
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 增加可选 Disk MoE 分段耗时 profile。默认关闭；设置 `FASTLLM_DISK_MOE_PROFILE=1` 后，会按 `FASTLLM_DISK_MOE_PROFILE_EVERY` 间隔打印 selection、load、prepare、input convert、CPU merge、output convert、cleanup 的平均耗时。
- 构建验证：`cmd /c build_cuda_fastllm_clean.cmd` 已成功构建 clean DLL；`git diff --check` 通过。
- 开启 profile 的 16 token 诊断：日志 `资料\验证结果\q8_long_decode_20260511_014253.log`。结果 `done_sec=104.2`，D 盘平均读速约 `1163.7 MB/s`、最后 30 样本平均约 `1272.2 MB/s`。
- profile 结论：16 token 末尾累计 `calls=768`、`avgLoadWeights=18.70`。每次 Disk MoE 平均总耗时约 `87.680ms`，其中 `load=77.571ms`，`merge=10.056ms`，selection、prepare、输入输出转换和 cleanup 都接近零头。说明当前最主要瓶颈不是 topk 数量，也不是 CPU merge 本身，而是 Disk MoE 权重加载路径。
- 关闭 profile 的默认复测：日志 `资料\验证结果\q8_long_decode_20260511_014641.log`。结果 `loaded_sec=19.7`、`done_sec=102.5`，D 盘平均读速约 `1189.0 MB/s`、最大约 `2137.4 MB/s`、最后 30 样本平均约 `1294.7 MB/s`。关闭态没有可见性能回退。
- 判断：这轮主要是定位瓶颈，不是降低质量换速度。DeepSeek-V4-Flash 仍保持官方 `num_experts_per_tok=6` / 本地 GGUF `expert_used_count=6`，没有改 Q8 精度，没有改采样策略。
- 下一步方向：继续优化应优先围绕减少 Disk MoE miss 读盘量、改善加载批次形态、降低每次权重加载分配/拷贝成本，而不是盲目增加线程或降低专家数。
- 回看此版本：`git show 1f9bc17`
- 从此版本建分支：`git switch -c 分支名 1f9bc17`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 1f9bc17`

## Q8 Disk MoE 加载任务栈上分配微优化

- 时间：2026-05-11 01:55:22 +01:00
- 本地提交哈希：`89693e9b56ad7324fb81972e9b849ee07979c86a`
- 短哈希：`89693e9`
- 本地标签：暂未打标签
- 改动：`fastllm-master\src\devices\disk\diskdevice.cpp` 中 Disk MoE 并行加载任务从每次 `new/delete LoadDiskWeightsOp` 改成当前调用内的 `std::vector<LoadDiskWeightsOp>` 栈生命周期对象，减少每次 MoE load 阶段的小堆分配。
- 构建验证：`cmd /c build_cuda_fastllm_clean.cmd` 成功，生成 clean DLL；`git diff --check` 通过。
- 默认 16 token 复测：关闭 stats/profile，日志 `资料\验证结果\q8_long_decode_20260511_015257.log`。结果 `loaded_sec=19.5`、`done_sec=101.7`，D 盘平均读速约 `1230.8 MB/s`、最大约 `1924.1 MB/s`、最后 30 样本平均约 `1377.5 MB/s`。
- 判断：相对上一轮关闭 profile 默认复测 `done_sec=102.5` 和 offset ReadFile 最好 `done_sec=101.8`，这轮属于基本持平、略正向的小优化；它的价值主要是去掉热点路径上的无意义堆分配，不是解决 Disk MoE miss 读盘量的根本方案。
- 质量约束：没有改 `num_experts_per_tok`，没有降低 Q8 精度，没有改采样策略。
- 回看此版本：`git show 89693e9`
- 从此版本建分支：`git switch -c 分支名 89693e9`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 89693e9`

## Codex Q8 优化对话脱敏归档

- 时间：2026-05-11 06:24:28 +01:00
- 本地提交哈希：`85407498720f325545949557a8ae3d9845a28ec9`
- 短哈希：`8540749`
- 本地标签：暂未打标签
- 改动：新增 `资料\与 codex 对话-优化记录\2026-05-10_检查-q8-模型输出_优化对话.sanitized.jsonl` 和 `资料\与 codex 对话-优化记录\2026-05-10_检查-q8-模型输出_优化对话.readable.md`，归档本轮 Q8 / Disk MoE 优化对话和工具调用链。
- 来源：Codex 会话 `%USERPROFILE%\.codex\sessions\2026\05\10\rollout-2026-05-10T15-07-07-019e1236-947b-7661-a534-be06b0b37118.jsonl`。
- 导出范围：源会话第 1 行到第 4001 行；第 4002 行开始是归档脚本自身调用，未纳入，避免把生成归档的脚本和后续自检递归写进归档。
- 脱敏处理：替换 `%USERPROFILE%` 下本机账号路径、常见 token / API key / Bearer / GitHub / HF / OpenAI token / 邮箱 / OTP URI 模式；替换 `encrypted_content` 加密推理块；替换 base instructions、developer 消息和 developer instructions。
- 验证：脱敏 JSONL 共 `4001` 行，JSON 解析错误 `0`；使用 `rg` 扫描真实 Windows 用户目录、OTP URI、OpenAI/GitHub/HF token、Bearer 等常见敏感模式，无命中。
- 说明：这是资料归档提交，不改变模型运行代码，不影响当前 Q8 稳定版本性能。
- 回看此版本：`git show 8540749`
- 从此版本建分支：`git switch -c 分支名 8540749`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 8540749`

## 本地 OpenAI API 调用器

- 时间：2026-05-11 07:59:38 +01:00
- 本地提交哈希：`7ae966e5d604f9a67e3d7cfbe57290f6c0ed3c45`
- 短哈希：`7ae966e`
- 本地标签：暂未打标签
- 改动：新增 `call_openai_api.py`，用 Python 标准库直接调用本地 `openai_compatible_server.py` 的 `/v1/chat/completions`，用于绕开 Cherry Studio 客户端变量，排查长上下文请求是否被客户端超时或断流影响。
- 功能：默认流式输出、默认 `max_tokens=16`、默认超时 `3600` 秒；支持 `--prompt`、`--prompt-file`、stdin、`--output-file`、`--no-stream`、`--health-first/--no-health-first`、`--show-request`。
- 诊断输出：模型文本输出到 stdout；请求信息、health、HTTP 首事件时间、首内容时间、每个 chunk 时间和最终 SUMMARY 输出到 stderr，方便区分客户端超时、服务端断连和正常结束。
- 验证：`python -m py_compile .\call_openai_api.py` 通过；`python .\call_openai_api.py --help` 通过；`git diff --cached --check` 通过。
- 说明：本次不改模型加载、Disk MoE、KV cache 或推理逻辑，只新增调用工具；不影响当前 Q8 性能基线。
- 回看此版本：`git show 7ae966e`
- 从此版本建分支：`git switch -c 分支名 7ae966e`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 7ae966e`

## 本地 API 调用器防闪退 bat 启动脚本

- 时间：2026-05-11 08:04:41 +01:00
- 本地提交哈希：`ae5d89aa76b7cbd76724c24b80cd257a542bd443`
- 短哈希：`ae5d89a`
- 本地标签：暂未打标签
- 改动：新增 `run_call_openai_api.bat`，用于启动 `call_openai_api.py` 后自动 `pause`，避免直接运行 Python 脚本时窗口闪退看不到输出。
- 默认行为：不传参数时运行 `call_openai_api.py --max-tokens 16 --timeout 7200`；传参数时原样转发给 `call_openai_api.py`，例如 `run_call_openai_api.bat --prompt "你好" --max-tokens 16`。
- 兼容处理：脚本先找 `python`，找不到时尝试 `py -3`；设置 `chcp 65001` 并切换到脚本所在目录，减少中文路径和工作目录问题。
- 验证：`cmd /c run_call_openai_api.bat --help` 成功显示调用器帮助并停在 pause；`git diff --cached --check` 通过。
- 说明：本次不改服务端、模型或 API 调用器逻辑，只新增 Windows 启动封装，解决易用性问题。
- 回看此版本：`git show ae5d89a`
- 从此版本建分支：`git switch -c 分支名 ae5d89a`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard ae5d89a`

## 本地 API 调用器默认安静输出

- 时间：2026-05-11 08:12:38 +01:00
- 本地提交哈希：`2898ab72049dea252e3fb99492e69a202547db0f`
- 短哈希：`2898ab7`
- 本地标签：暂未打标签
- 改动：`call_openai_api.py` 新增 `--verbose`，默认不再打印 `REQUEST`、`HEALTH`、`HTTP`、`FIRST_CONTENT_SEC`、`CHUNK`、`STREAM_DONE`、`SUMMARY` 等诊断日志；诊断信息需要时再手动加 `--verbose`。
- bat 调整：`run_call_openai_api.bat` 默认不再打印启动说明，只调用 API、显示模型正文、最后显示退出码并 `pause`。
- 保留改动：保留当前默认 prompt `你好呀，你是谁呀？`，没有改回旧提示。
- 验证：`python -m py_compile .\call_openai_api.py` 通过；`python .\call_openai_api.py --help` 通过；`cmd /c run_call_openai_api.bat --help` 通过；`python .\call_openai_api.py --prompt "只输出一个字：好" --max-tokens 1 --timeout 180` 实测只输出 `好`，不再混入 chunk 日志。
- 说明：本次只修正调用器输出体验，不改服务端、模型加载或 Disk MoE 推理逻辑。
- 回看此版本：`git show 2898ab7`
- 从此版本建分支：`git switch -c 分支名 2898ab7`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 2898ab7`

## 移除本地 API 默认输出 token 限制

- 时间：2026-05-11 08:21:26 +01:00
- 本地提交哈希：`3041699afc8d39e216278a5dbf7d936fba57e220`
- 短哈希：`3041699`
- 本地标签：暂未打标签
- 改动：`call_openai_api.py` 的 `--max-tokens` 默认值改为 `None`，默认请求不再发送 `max_tokens` 字段；只有手动传 `--max-tokens 16` 等参数时才限制输出长度。
- bat 调整：`run_call_openai_api.bat` 默认只传 `--timeout 7200`，不再传 `--max-tokens`。
- 服务端调整：`model_runtime.py` 在请求没有 `max_tokens` 时不再用 `default_max_tokens=128` 补默认上限，而是把 `max_length=None` 交给底层生成；请求明确传 `max_tokens` 时仍按传入值限制。
- 验证：`python -m py_compile .\call_openai_api.py .\model_runtime.py` 通过；`python .\call_openai_api.py --help` 通过；小脚本确认默认 payload 不含 `max_tokens`，手动设置时才包含 `16`；`git diff --cached --check` 通过。
- 说明：本次目标是删除客户端和服务端默认输出截断，不改变超时设置、不改变 Disk MoE、KV cache、采样参数或模型精度。
- 测速用法：以后需要固定 16 token 测速时手动运行 `run_call_openai_api.bat --max-tokens 16` 或 `python .\call_openai_api.py --max-tokens 16`。
- 回看此版本：`git show 3041699`
- 从此版本建分支：`git switch -c 分支名 3041699`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard 3041699`

## 本地 API 调用器远程断连友好提示

- 时间：2026-05-11 08:30:52 +01:00
- 本地提交哈希：`b246fd6ef5bea40a4381da548b4b315c6b82412e`
- 短哈希：`b246fd6`
- 本地标签：暂未打标签
- 改动：`call_openai_api.py` 捕获 `BrokenPipeError`、`ConnectionAbortedError`、`ConnectionResetError`，远程连接被服务端重置时输出 `REMOTE_CLOSED ...` 并返回退出码 `5`，不再打印 Python traceback。
- 背景：默认 token 限制移除后，调用器可以继续接收更长输出；如果服务端或底层推理进程中途关闭连接，客户端应保留已输出文本并给出清楚的一行错误。
- 验证：`python -m py_compile .\call_openai_api.py` 通过；`python .\call_openai_api.py --help` 通过；`git diff --cached --check` 通过。
- 说明：本次只改客户端错误显示，不修复服务端为何重置连接；下一步需要看服务端窗口是否有崩溃、异常或底层 fastllm 退出日志。
- 回看此版本：`git show b246fd6`
- 从此版本建分支：`git switch -c 分支名 b246fd6`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard b246fd6`

## Q8 OpenAI API 长流式稳定版

- 时间：2026-05-12 01:22:38 +01:00
- 本地提交哈希：`96267381827d7f9c77bcc9d7614b25c587feea86`
- 短哈希：`9626738`
- 本地标签：`q8-openai-api-128kv-stable`
- 改动：修复 DeepSeekV4 在 128 KV/past cache 边界附近触发服务端断开的问题；`call_openai_api.py` 保持最简单的本地 OpenAI API 流式请求，不发送 `max_tokens`；服务端默认长输出上限调整为 `1000000`，用于暴力长流式测试时不由 Python 侧提前截断。
- 服务端行为：`openai_compatible_server.py` 启动后先监听端口，再后台加载模型；流式响应使用 OpenAI 兼容 chunk；异常时返回 SSE error 并结束流；连接使用 `Connection: close`，避免长连接复用干扰。
- 运行配置：`model_runtime.py` 保持 `model.set_verbose(1)`，继续输出 fastllm decode 日志；`default_max_tokens=1000000`；`llm.set_max_tokens(self.default_max_tokens)` 与流式 `max_length` 口径一致。
- 验收：用户已手动测试当前版本，反馈没有明显问题；此前用 `写一篇1000字小说` 验收过完整长流式输出，客户端退出码 `0`、stderr 长度 `0`、输出长度 `1582` 字符，结尾完整，没有 `ConnectionResetError 10054`。
- 说明：C++ 改动不是新增输出上限，而是解除之前 128 边界 bug 导致的服务端崩溃限制；真实长跑上限仍交给 KV cache、fastllm 引擎和运行环境。
- 回看此版本：`git show 9626738` 或 `git show q8-openai-api-128kv-stable`
- 从此版本建分支：`git switch -c 分支名 q8-openai-api-128kv-stable`
- 回退到此版本：先确认没有未保存改动，再执行 `git switch main`，然后按需要使用 `git reset --hard q8-openai-api-128kv-stable`
